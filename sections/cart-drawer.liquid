<style>
  .drawer {
    visibility: hidden;
  }
</style>

{%- liquid
  assign heading_text = section.settings.heading_text | default: 'Cart'
  assign heading_alignment = section.settings.heading_alignment
  assign desktop_width = section.settings.desktop_width
  assign mobile_width = section.settings.mobile_width
-%}

{% style %}
  /* Width */
  {% if desktop_width == 'large' %}
    .drawer__inner { width: 48rem; }
  {% endif %}
  {% if mobile_width == 'full' %}
    @media screen and (max-width: 749px) {
      .drawer__inner { max-width: 100vw; }
    }
  {% endif %}

  /* Heading alignment */
  {% if heading_alignment == 'center' %}
    .drawer__header { justify-content: center; }
    .drawer__heading { text-align: center; flex: 1; }
  {% endif %}

  /* Custom background: header */
  {% if section.settings.header_custom_bg %}
    .drawer__header {
      background: {{ section.settings.header_bg_color }};
      margin: 0 -1.5rem;
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }
  {% endif %}

  /* Custom background: body */
  {% if section.settings.body_custom_bg %}
    cart-drawer-items {
      background: {{ section.settings.body_bg_color }};
      margin: 0 -1.5rem;
      padding: 0 1.5rem;
    }
  {% endif %}

  /* Custom background: footer */
  {% if section.settings.footer_custom_bg %}
    .drawer__footer {
      background: {{ section.settings.footer_bg_color }};
      margin: 0 -1.5rem;
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }
  {% endif %}
{% endstyle %}

<cart-drawer class="drawer{% if cart == empty %} is-empty{% endif %}">
  <div id="CartDrawer" class="cart-drawer">
    <div id="CartDrawer-Overlay" class="cart-drawer__overlay"></div>
    <div
      class="drawer__inner gradient color-{{ section.settings.color_scheme }}"
      role="dialog"
      aria-modal="true"
      aria-label="{{ heading_text | replace: '[count]', cart.item_count }}"
      tabindex="-1"
    >
      {%- if cart == empty -%}
        <div class="drawer__inner-empty">
          <div class="cart-drawer__warnings center{% if settings.cart_drawer_collection != blank %} cart-drawer__warnings--has-collection{% endif %}">
            <div class="cart-drawer__empty-content">
              <h2 class="cart__empty-text">{{ 'sections.cart.empty' | t }}</h2>
              <button
                class="drawer__close"
                type="button"
                onclick="this.closest('cart-drawer').close()"
                aria-label="{{ 'accessibility.close' | t }}"
              >
                <span class="svg-wrapper">
                  {{- 'icon-close.svg' | inline_asset_content -}}
                </span>
              </button>
              <a href="{{ routes.all_products_collection_url }}" class="button">
                {{ 'general.continue_shopping' | t }}
              </a>

              {%- if shop.customer_accounts_enabled and customer == null -%}
                <p class="cart__login-title h3">{{ 'sections.cart.login.title' | t }}</p>
                <p class="cart__login-paragraph">
                  {{ 'sections.cart.login.paragraph_html' | t: link: routes.account_login_url }}
                </p>
              {%- endif -%}
            </div>
          </div>
          {%- if settings.cart_drawer_collection != blank -%}
            <div class="cart-drawer__collection">
              {% render 'card-collection', card_collection: settings.cart_drawer_collection, columns: 1 %}
            </div>
          {%- endif -%}
        </div>
      {%- endif -%}

      <div class="drawer__header">
        <h2 class="drawer__heading">{{ heading_text | replace: '[count]', cart.item_count }}</h2>
        <button
          class="drawer__close"
          type="button"
          onclick="this.closest('cart-drawer').close()"
          aria-label="{{ 'accessibility.close' | t }}"
        >
          <span class="svg-wrapper">
            {{- 'icon-close.svg' | inline_asset_content -}}
          </span>
        </button>
      </div>

      {%- comment -%} Detect structural blocks {%- endcomment -%}
      {%- liquid
        assign has_cart_items_block = false
        assign has_checkout_block = false
        for block in section.blocks
          if block.type == 'cart_items'
            assign has_cart_items_block = true
          endif
          if block.type == 'checkout_button'
            assign has_checkout_block = true
          endif
        endfor
      -%}

      {%- comment -%} BODY BLOCKS before cart items {%- endcomment -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'cart_items' -%}{%- break -%}{%- endif -%}
        {%- if block.type == 'checkout_button' -%}{%- continue -%}{%- endif -%}
        {%- liquid
          assign block_position = block.settings.position | default: 'footer'
          if block.type == 'free_shipping_bar' and block.settings.position == blank
            assign block_position = 'body'
          endif
        -%}
        {%- if block_position == 'body' -%}
          {%- render 'cart-drawer-block', block: block, cart: cart -%}
        {%- endif -%}
      {%- endfor -%}

      <cart-drawer-items
        {% if cart == empty %}
          class=" is-empty"
        {% endif %}
      >
        <form
          action="{{ routes.cart_url }}"
          id="CartDrawer-Form"
          class="cart__contents cart-drawer__form"
          method="post"
        >
          <div id="CartDrawer-CartItems" class="drawer__contents js-contents">
            {%- if cart != empty -%}
              <div class="drawer__cart-items-wrapper">
                <table class="cart-items" role="table">
                  <thead class="visually-hidden" role="rowgroup">
                    <tr role="row">
                      <th id="CartDrawer-ColumnProductImage" role="columnheader">
                        {{ 'sections.cart.headings.image' | t }}
                      </th>
                      <th id="CartDrawer-ColumnProduct" scope="col" role="columnheader">
                        {{ 'sections.cart.headings.product' | t }}
                      </th>
                      <th id="CartDrawer-ColumnTotal" scope="col" role="columnheader">
                        {{ 'sections.cart.headings.total' | t }}
                      </th>
                      <th id="CartDrawer-ColumnQuantity" role="columnheader">
                        {{ 'sections.cart.headings.quantity' | t }}
                      </th>
                    </tr>
                  </thead>

                  <tbody role="rowgroup">
                    {%- for item in cart.items -%}
                      <tr
                        id="CartDrawer-Item-{{ item.index | plus: 1 }}"
                        class="cart-item{% if item.parent_relationship.parent != null %} cart-item__nested-line{% endif %}"
                        role="row"
                        {% if item.parent_relationship.parent != null %}
                          aria-label="{{- 'products.product.nested_label' | t: title: item.product.title, parent_title: item.parent_relationship.parent.title | escape -}}"
                        {% endif %}
                      >
                        <td
                          class="cart-item__media"
                          role="cell"
                          headers="CartDrawer-ColumnProductImage"
                        >
                          {% if item.image %}
                            {% comment %} Leave empty space due to a:empty CSS display: none rule {% endcomment %}
                            <a href="{{ item.url }}" class="cart-item__link" tabindex="-1" aria-hidden="true"> </a>
                            <img
                              class="cart-item__image"
                              src="{{ item.image | image_url: width: 300 }}"
                              alt="{{ item.image.alt | escape }}"
                              loading="lazy"
                              width="150"
                              height="{{ 150 | divided_by: item.image.aspect_ratio | ceil }}"
                            >
                          {% endif %}
                        </td>

                        <td class="cart-item__details" role="cell" headers="CartDrawer-ColumnProduct">
                          {%- if settings.show_vendor -%}
                            <p class="caption-with-letter-spacing light">{{ item.product.vendor }}</p>
                          {%- endif -%}

                          <a href="{{ item.url }}" class="cart-item__name h4 break">
                            {{- item.product.title | escape -}}
                          </a>

                          {%- if item.original_price != item.final_price -%}
                            <div class="cart-item__discounted-prices">
                              <span class="visually-hidden">
                                {{ 'products.product.price.regular_price' | t }}
                              </span>
                              <s class="cart-item__old-price product-option">
                                {{- item.original_price | money -}}
                              </s>
                              <span class="visually-hidden">
                                {{ 'products.product.price.sale_price' | t }}
                              </span>
                              <strong class="cart-item__final-price product-option">
                                {{ item.final_price | money }}
                              </strong>
                            </div>
                          {%- else -%}
                            <div class="product-option">
                              {{ item.original_price | money }}
                            </div>
                          {%- endif -%}

                          {%- if item.product.has_only_default_variant == false
                            or item.properties.size != 0
                            or item.selling_plan_allocation != null
                          -%}
                            <dl>
                              {%- if item.product.has_only_default_variant == false -%}
                                {%- for option in item.options_with_values -%}
                                  <div class="product-option">
                                    <dt>{{ option.name }}:</dt>
                                    <dd>
                                      {{ option.value -}}
                                      {%- unless forloop.last %}, {% endunless %}
                                    </dd>
                                  </div>
                                {%- endfor -%}
                              {%- endif -%}

                              {%- for property in item.properties -%}
                                {%- assign property_first_char = property.first | slice: 0 -%}
                                {%- if property.last != blank and property_first_char != '_' -%}
                                  <div class="product-option">
                                    <dt>{{ property.first }}:</dt>
                                    <dd>
                                      {%- if property.last contains '/uploads/' -%}
                                        <a
                                          href="{{ property.last }}"
                                          class="link"
                                          target="_blank"
                                          aria-describedby="a11y-new-window-message"
                                        >
                                          {{ property.last | split: '/' | last }}
                                        </a>
                                      {%- else -%}
                                        {{ property.last }}
                                      {%- endif -%}
                                    </dd>
                                  </div>
                                {%- endif -%}
                              {%- endfor -%}
                            </dl>

                            <p class="product-option">{{ item.selling_plan_allocation.selling_plan.name }}</p>
                          {%- endif -%}

                          <ul
                            class="discounts list-unstyled"
                            role="list"
                            aria-label="{{ 'customer.order.discount' | t }}"
                          >
                            {%- for discount in item.line_level_discount_allocations -%}
                              <li class="discounts__discount">
                                {{- 'icon-discount.svg' | inline_asset_content -}}
                                {{ discount.discount_application.title }}
                              </li>
                            {%- endfor -%}
                          </ul>
                        </td>

                        <td class="cart-item__totals right" role="cell" headers="CartDrawer-ColumnTotal">
                          {%- render 'loading-spinner' -%}
                          <div class="cart-item__price-wrapper">
                            {%- if item.original_line_price != item.final_line_price -%}
                              <div class="cart-item__discounted-prices">
                                <span class="visually-hidden">
                                  {{ 'products.product.price.regular_price' | t }}
                                </span>
                                <s class="cart-item__old-price price price--end">
                                  {{ item.original_line_price | money }}
                                </s>
                                <span class="visually-hidden">
                                  {{ 'products.product.price.sale_price' | t }}
                                </span>
                                <span class="price price--end">
                                  {{ item.final_line_price | money }}
                                </span>
                              </div>
                            {%- else -%}
                              <span class="price price--end">
                                {{ item.original_line_price | money }}
                              </span>
                            {%- endif -%}

                            {%- if item.variant.available and item.unit_price_measurement -%}
                              {% render 'unit-price', price: item.unit_price, measurement: item.unit_price_measurement %}
                            {%- endif -%}
                          </div>
                        </td>
                        {%- liquid
                          assign has_qty_rules = false
                          if item.variant.quantity_rule.increment > 1 or item.variant.quantity_rule.min > 1 or item.variant.quantity_rule.max != null
                            assign has_qty_rules = true
                          endif

                          assign has_vol_pricing = false
                          if item.variant.quantity_price_breaks.size > 0
                            assign has_vol_pricing = true
                          endif
                        -%}
                        <td
                          class="cart-item__quantity {% if has_qty_rules or has_vol_pricing %} cart-item__quantity--info{% endif %}"
                          role="cell"
                          headers="CartDrawer-ColumnQuantity"
                        >
                          <quantity-popover>
                            <div class="cart-item__quantity-wrapper quantity-popover-wrapper">
                              <div class="quantity-popover-container{% if has_qty_rules or has_vol_pricing %} quantity-popover-container--hover{% endif %}">
                                <quantity-input class="quantity cart-quantity">
                                  <button
                                    class="quantity__button"
                                    name="minus"
                                    type="button"
                                    {% if item.instructions and item.instructions.can_update_quantity == false %}
                                      disabled
                                    {% endif %}
                                  >
                                    <span class="visually-hidden">
                                      {{-
                                        'products.product.quantity.decrease'
                                        | t: product: item.product.title
                                        | escape
                                      -}}
                                    </span>
                                    <span class="svg-wrapper">
                                      {{- 'icon-minus.svg' | inline_asset_content -}}
                                    </span>
                                  </button>
                                  <input
                                    class="quantity__input"
                                    type="number"
                                    data-quantity-variant-id="{{ item.variant.id }}"
                                    name="updates[]"
                                    value="{{ item.quantity }}"
                                    {% # theme-check-disable %}
                                    data-cart-quantity="{{ cart | item_count_for_variant: item.variant.id }}"
                                    min="0"
                                    data-min="{{ item.variant.quantity_rule.min }}"
                                    {% if item.variant.quantity_rule.max != null %}
                                      max="{{ item.variant.quantity_rule.max }}"
                                    {% endif %}
                                    step="{{ item.variant.quantity_rule.increment }}"
                                    {% # theme-check-enable %}
                                    aria-label="{{ 'products.product.quantity.input_label' | t: product: item.product.title | escape }}"
                                    id="Drawer-quantity-{{ item.index | plus: 1 }}"
                                    data-index="{{ item.index | plus: 1 }}"
                                    {% if item.instructions and item.instructions.can_update_quantity == false %}
                                      disabled
                                    {% endif %}
                                  >
                                  <button
                                    class="quantity__button"
                                    name="plus"
                                    type="button"
                                    {% if item.instructions and item.instructions.can_update_quantity == false %}
                                      disabled
                                    {% endif %}
                                  >
                                    <span class="visually-hidden">
                                      {{-
                                        'products.product.quantity.increase'
                                        | t: product: item.product.title
                                        | escape
                                      -}}
                                    </span>
                                    <span class="svg-wrapper">
                                      {{- 'icon-plus.svg' | inline_asset_content -}}
                                    </span>
                                  </button>
                                </quantity-input>
                              </div>
                              <cart-remove-button
                                id="CartDrawer-Remove-{{ item.index | plus: 1 }}"
                                data-index="{{ item.index | plus: 1 }}"
                              >
                                <button
                                  type="button"
                                  class="button button--tertiary cart-remove-button"
                                  aria-label="{{ 'sections.cart.remove_title' | t: title: item.title | escape }}"
                                  data-variant-id="{{ item.variant.id }}"
                                  {% if item.instructions and item.instructions.can_remove == false %}
                                    class="hidden"
                                  {% endif %}
                                >
                                  <span class="svg-wrapper">
                                    {{- 'icon-remove.svg' | inline_asset_content -}}
                                  </span>
                                </button>
                              </cart-remove-button>
                            </div>
                            {%- if has_qty_rules or has_vol_pricing -%}
                              <button
                                type="button"
                                class="quantity-popover__info-button quantity-popover__info-button--icon-with-label button button--tertiary"
                                aria-expanded="false"
                              >
                                <span class="svg-wrapper">
                                  {{- 'icon-info.svg' | inline_asset_content -}}
                                </span>
                                <span>
                                  {%- if has_vol_pricing -%}
                                    {{ 'products.product.volume_pricing.note' | t }}
                                  {%- elsif has_qty_rules -%}
                                    {{ 'products.product.quantity.note' | t }}
                                  {%- endif -%}
                                </span>
                              </button>
                            {%- endif -%}
                            {%- if has_vol_pricing or has_qty_rules -%}
                              <div
                                class="cart-items__info global-settings-popup quantity-popover__info"
                                tabindex="-1"
                                hidden
                              >
                                {%- if has_qty_rules == false -%}
                                  <span class="volume-pricing-label caption">
                                    {{- 'products.product.volume_pricing.title' | t -}}
                                  </span>
                                {%- endif -%}
                                <div class="quantity__rules caption">
                                  {%- if item.variant.quantity_rule.increment > 1 -%}
                                    <span class="divider">
                                      {{-
                                        'products.product.quantity.multiples_of'
                                        | t: quantity: item.variant.quantity_rule.increment
                                      -}}
                                    </span>
                                  {%- endif -%}
                                  {%- if item.variant.quantity_rule.min > 1 -%}
                                    <span class="divider">
                                      {{-
                                        'products.product.quantity.min_of'
                                        | t: quantity: item.variant.quantity_rule.min
                                      -}}
                                    </span>
                                  {%- endif -%}
                                  {%- if item.variant.quantity_rule.max != null -%}
                                    <span class="divider">
                                      {{-
                                        'products.product.quantity.max_of'
                                        | t: quantity: item.variant.quantity_rule.max
                                      -}}
                                    </span>
                                  {%- endif -%}
                                </div>
                                <button
                                  class="button-close button button--tertiary"
                                  type="button"
                                  aria-label="{{ 'accessibility.close' | t }}"
                                >
                                  <span class="svg-wrapper">
                                    {{- 'icon-close.svg' | inline_asset_content -}}
                                  </span>
                                </button>
                                {%- if item.variant.quantity_price_breaks.size > 0 -%}
                                  <volume-pricing class="parent-display">
                                    <ul class="list-unstyled">
                                      <li>
                                        <span>{{ item.variant.quantity_rule.min }}+</span>
                                        <span>{{ item.variant.price | money_with_currency }}/ea</span>
                                      </li>
                                      {%- for price_break in item.variant.quantity_price_breaks -%}
                                        <li>
                                          <span>
                                            {{- price_break.minimum_quantity -}}
                                            <span aria-hidden="true">+</span></span
                                          >
                                          <span>{{ price_break.price | money_with_currency }}/ea</span>
                                        </li>
                                      {%- endfor -%}
                                    </ul>
                                  </volume-pricing>
                                {%- endif -%}
                              </div>
                            {%- endif -%}
                            <div
                              id="CartDrawer-LineItemError-{{ item.index | plus: 1 }}"
                              class="cart-item__error"
                              role="alert"
                            >
                              <small class="cart-item__error-text"></small>
                              <span class="svg-wrapper">
                                {{- 'icon-error.svg' | inline_asset_content -}}
                              </span>
                            </div>
                          </quantity-popover>
                        </td>
                      </tr>
                    {%- endfor -%}
                  </tbody>
                </table>
              </div>
            {%- endif -%}
            <p id="CartDrawer-LiveRegionText" class="visually-hidden" role="status"></p>
            <p id="CartDrawer-LineItemStatus" class="visually-hidden" aria-hidden="true" role="status">
              {{ 'accessibility.loading' | t }}
            </p>
          </div>
          <div id="CartDrawer-CartErrors" role="alert"></div>
        </form>
      </cart-drawer-items>

      {%- comment -%} BODY BLOCKS after cart items (only when cart_items block exists) {%- endcomment -%}
      {%- if has_cart_items_block -%}
        {%- assign past_cart_items_block = false -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'cart_items' -%}
            {%- assign past_cart_items_block = true -%}
            {%- continue -%}
          {%- endif -%}
          {%- unless past_cart_items_block -%}{%- continue -%}{%- endunless -%}
          {%- if block.type == 'checkout_button' -%}{%- continue -%}{%- endif -%}
          {%- liquid
            assign block_position = block.settings.position | default: 'footer'
            if block.type == 'free_shipping_bar' and block.settings.position == blank
              assign block_position = 'body'
            endif
          -%}
          {%- if block_position == 'body' -%}
            {%- render 'cart-drawer-block', block: block, cart: cart -%}
          {%- endif -%}
        {%- endfor -%}
      {%- endif -%}

      <div class="drawer__footer">
        {%- if section.settings.show_cart_note -%}
          <details id="Details-CartDrawer">
            <summary>
              <span class="summary__title">
                {{ 'sections.cart.note' | t }}
                {{- 'icon-caret.svg' | inline_asset_content -}}
              </span>
            </summary>
            <cart-note class="cart__note field">
              <label class="visually-hidden" for="CartDrawer-Note">{{ 'sections.cart.note' | t }}</label>
              <textarea
                id="CartDrawer-Note"
                class="text-area text-area--resize-vertical field__input"
                name="note"
                placeholder="{{ 'sections.cart.note' | t }}"
              >{{ cart.note }}</textarea>
            </cart-note>
          </details>
        {%- endif -%}

        {%- comment -%} Capture checkout + totals for reuse {%- endcomment -%}
        {%- capture checkout_section_html -%}
        <div class="cart-drawer__footer">
          <div>
            {%- if cart.cart_level_discount_applications.size > 0 -%}
              <ul class="discounts list-unstyled" role="list" aria-label="{{ 'customer.order.discount' | t }}">
                {%- for discount in cart.cart_level_discount_applications -%}
                  <li class="discounts__discount discounts__discount--end">
                    {{- 'icon-discount.svg' | inline_asset_content -}}
                    {{ discount.title | escape }}
                    (-{{ discount.total_allocated_amount | money }})
                  </li>
                {%- endfor -%}
              </ul>
            {%- endif -%}
          </div>

          <div class="totals" role="status">
            <h2 class="totals__total">{{ 'sections.cart.estimated_total' | t }}</h2>
            <p class="totals__total-value">{{ cart.total_price | money_with_currency }}</p>
          </div>

        </div>

        <div class="cart__ctas">
          <button
            type="submit"
            id="CartDrawer-Checkout"
            class="cart__checkout-button button"
            name="checkout"
            form="CartDrawer-Form"
            {% if cart == empty %}
              disabled
            {% endif %}
          >
            {{ 'sections.cart.checkout' | t }}
          </button>
          {%- if section.settings.show_dynamic_checkout -%}
            <div class="cart__dynamic-checkout-buttons additional-checkout-buttons">
              {{ content_for_additional_checkout_buttons }}
            </div>
          {%- endif -%}
        </div>
        {%- endcapture -%}

        {%- unless has_checkout_block -%}
          {%- comment -%} Default: checkout first, then footer blocks below {%- endcomment -%}
          {{ checkout_section_html }}
          {%- for block in section.blocks -%}
            {%- if block.type == 'cart_items' or block.type == 'checkout_button' -%}{%- continue -%}{%- endif -%}
            {%- liquid
              assign block_position = block.settings.position | default: 'footer'
              if block.type == 'free_shipping_bar' and block.settings.position == blank
                assign block_position = 'body'
              endif
            -%}
            {%- if block_position == 'footer' -%}
              {%- render 'cart-drawer-block', block: block, cart: cart -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endunless -%}

        {%- if has_checkout_block -%}
          {%- comment -%} Custom order: footer blocks around checkout_button {%- endcomment -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'checkout_button' -%}{%- break -%}{%- endif -%}
            {%- if block.type == 'cart_items' -%}{%- continue -%}{%- endif -%}
            {%- liquid
              assign block_position = block.settings.position | default: 'footer'
              if block.type == 'free_shipping_bar' and block.settings.position == blank
                assign block_position = 'body'
              endif
            -%}
            {%- if block_position == 'footer' -%}
              {%- render 'cart-drawer-block', block: block, cart: cart -%}
            {%- endif -%}
          {%- endfor -%}

          {{ checkout_section_html }}

          {%- assign past_checkout_block = false -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'checkout_button' -%}
              {%- assign past_checkout_block = true -%}
              {%- continue -%}
            {%- endif -%}
            {%- unless past_checkout_block -%}{%- continue -%}{%- endunless -%}
            {%- if block.type == 'cart_items' -%}{%- continue -%}{%- endif -%}
            {%- liquid
              assign block_position = block.settings.position | default: 'footer'
              if block.type == 'free_shipping_bar' and block.settings.position == blank
                assign block_position = 'body'
              endif
            -%}
            {%- if block_position == 'footer' -%}
              {%- render 'cart-drawer-block', block: block, cart: cart -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  </div>
</cart-drawer>


{% schema %}
{
  "name": "Cart drawer",
  "settings": [
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "text",
      "id": "heading_text",
      "label": "Text",
      "info": "Use [count] to display total amount of items in the cart.",
      "default": "Cart"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "Width"
    },
    {
      "type": "select",
      "id": "desktop_width",
      "label": "Desktop width",
      "options": [
        { "value": "normal", "label": "Normal" },
        { "value": "large", "label": "Large" }
      ],
      "default": "normal"
    },
    {
      "type": "select",
      "id": "mobile_width",
      "label": "Mobile width",
      "options": [
        { "value": "default", "label": "Default" },
        { "value": "full", "label": "Full screen width" }
      ],
      "default": "full"
    },
    {
      "type": "header",
      "content": "Cart header"
    },
    {
      "type": "checkbox",
      "id": "header_custom_bg",
      "label": "Enable custom background",
      "default": false
    },
    {
      "type": "color",
      "id": "header_bg_color",
      "label": "Custom background color",
      "default": "#F3F3F3"
    },
    {
      "type": "header",
      "content": "Cart body"
    },
    {
      "type": "checkbox",
      "id": "body_custom_bg",
      "label": "Enable custom background",
      "default": false
    },
    {
      "type": "color",
      "id": "body_bg_color",
      "label": "Custom background color",
      "default": "#F3F3F3"
    },
    {
      "type": "header",
      "content": "Cart footer"
    },
    {
      "type": "checkbox",
      "id": "footer_custom_bg",
      "label": "Enable custom background",
      "default": false
    },
    {
      "type": "color",
      "id": "footer_bg_color",
      "label": "Custom background color",
      "default": "#F3F3F3"
    },
    {
      "type": "header",
      "content": "Options"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "show_cart_note",
      "label": "Show cart note",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_dynamic_checkout",
      "label": "Show dynamic checkout buttons",
      "info": "Using the payment methods available on your store, customers see their preferred option, like PayPal or Apple Pay.",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "cart_items",
      "name": "Cart items",
      "limit": 1,
      "settings": []
    },
    {
      "type": "checkout_button",
      "name": "Checkout button",
      "limit": 1,
      "settings": []
    },
    {
      "type": "free_shipping_bar",
      "name": "Free shipping bar",
      "limit": 1,
      "settings": [
        {
          "type": "number",
          "id": "threshold",
          "label": "Free shipping threshold",
          "info": "Amount in your store's currency (e.g. 50 for $50)",
          "default": 50
        },
        {
          "type": "text",
          "id": "message",
          "label": "Progress message",
          "info": "Use [amount] for the remaining amount",
          "default": "You're [amount] away from free shipping!"
        },
        {
          "type": "text",
          "id": "success_message",
          "label": "Success message",
          "default": "You've unlocked free shipping!"
        },
        {
          "type": "color",
          "id": "bar_color",
          "label": "Bar color",
          "default": "#2E7D32"
        },
        {
          "type": "color",
          "id": "bg_color",
          "label": "Background color",
          "default": "#F1F8E9"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text color",
          "default": "#2E7D32"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "body"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        }
      ]
    },
    {
      "type": "countdown_timer",
      "name": "Countdown timer",
      "limit": 1,
      "settings": [
        {
          "type": "range",
          "id": "duration",
          "label": "Duration (minutes)",
          "min": 5,
          "max": 30,
          "step": 1,
          "unit": "min",
          "default": 10
        },
        {
          "type": "richtext",
          "id": "message",
          "label": "Message",
          "info": "Use [time] for the countdown timer",
          "default": "<p>Your cart is reserved for [time]</p>"
        },
        {
          "type": "text",
          "id": "expired_message",
          "label": "Expired message",
          "default": "Your reserved time has expired"
        },
        {
          "type": "header",
          "content": "Text size"
        },
        {
          "type": "range",
          "id": "text_size_desktop",
          "label": "Desktop text size",
          "min": 10,
          "max": 24,
          "step": 1,
          "unit": "px",
          "default": 14
        },
        {
          "type": "range",
          "id": "text_size_mobile",
          "label": "Mobile text size",
          "min": 10,
          "max": 24,
          "step": 1,
          "unit": "px",
          "default": 13
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "body"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "image",
      "name": "Image",
      "limit": 3,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "select",
          "id": "alignment",
          "label": "Alignment",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ],
          "default": "center"
        },
        {
          "type": "range",
          "id": "image_width",
          "label": "Image width",
          "min": 10,
          "max": 100,
          "step": 5,
          "unit": "%",
          "default": 100
        },
        {
          "type": "range",
          "id": "corner_radius",
          "label": "Corner radius",
          "min": 0,
          "max": 30,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (optional)"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "enhanced_text",
      "name": "Text with icons",
      "limit": 2,
      "settings": [
        {
          "type": "select",
          "id": "alignment",
          "label": "Alignment",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ],
          "default": "center"
        },
        {
          "type": "header",
          "content": "Item 1"
        },
        {
          "type": "select",
          "id": "icon_1",
          "label": "Icon",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "info", "label": "Info" },
            { "value": "truck", "label": "Shipping" },
            { "value": "shield", "label": "Shield" },
            { "value": "gift", "label": "Gift" },
            { "value": "returns", "label": "Returns" },
            { "value": "lock", "label": "Lock" },
            { "value": "check", "label": "Checkmark" },
            { "value": "clock", "label": "Clock" },
            { "value": "custom", "label": "Custom image" }
          ],
          "default": "truck"
        },
        {
          "type": "image_picker",
          "id": "icon_image_1",
          "label": "Custom icon image"
        },
        {
          "type": "richtext",
          "id": "text_1",
          "label": "Text",
          "default": "<p>Free shipping on orders over $50</p>"
        },
        {
          "type": "header",
          "content": "Item 2"
        },
        {
          "type": "select",
          "id": "icon_2",
          "label": "Icon",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "info", "label": "Info" },
            { "value": "truck", "label": "Shipping" },
            { "value": "shield", "label": "Shield" },
            { "value": "gift", "label": "Gift" },
            { "value": "returns", "label": "Returns" },
            { "value": "lock", "label": "Lock" },
            { "value": "check", "label": "Checkmark" },
            { "value": "clock", "label": "Clock" },
            { "value": "custom", "label": "Custom image" }
          ],
          "default": "none"
        },
        {
          "type": "image_picker",
          "id": "icon_image_2",
          "label": "Custom icon image"
        },
        {
          "type": "richtext",
          "id": "text_2",
          "label": "Text"
        },
        {
          "type": "header",
          "content": "Item 3"
        },
        {
          "type": "select",
          "id": "icon_3",
          "label": "Icon",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "info", "label": "Info" },
            { "value": "truck", "label": "Shipping" },
            { "value": "shield", "label": "Shield" },
            { "value": "gift", "label": "Gift" },
            { "value": "returns", "label": "Returns" },
            { "value": "lock", "label": "Lock" },
            { "value": "check", "label": "Checkmark" },
            { "value": "clock", "label": "Clock" },
            { "value": "custom", "label": "Custom image" }
          ],
          "default": "none"
        },
        {
          "type": "image_picker",
          "id": "icon_image_3",
          "label": "Custom icon image"
        },
        {
          "type": "richtext",
          "id": "text_3",
          "label": "Text"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "custom_liquid",
      "name": "Custom Liquid",
      "limit": 2,
      "settings": [
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "Liquid code",
          "info": "Add custom Liquid code to create advanced blocks."
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "trust_badges",
      "name": "Trust badges",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Secure Checkout"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "Badge image 1"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "Badge image 2"
        },
        {
          "type": "image_picker",
          "id": "image_3",
          "label": "Badge image 3"
        },
        {
          "type": "image_picker",
          "id": "image_4",
          "label": "Badge image 4"
        },
        {
          "type": "text",
          "id": "custom_text",
          "label": "Text below badges"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "text",
      "name": "Text / Announcement",
      "limit": 3,
      "settings": [
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Free returns within 30 days</p>"
        },
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "info", "label": "Info" },
            { "value": "truck", "label": "Shipping" },
            { "value": "shield", "label": "Shield" },
            { "value": "gift", "label": "Gift" },
            { "value": "returns", "label": "Returns" }
          ],
          "default": "truck"
        },
        {
          "type": "color",
          "id": "bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text color"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        }
      ]
    },
    {
      "type": "payment_badges",
      "name": "Payment badges",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "select",
          "id": "alignment",
          "label": "Alignment",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ],
          "default": "center"
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "range",
          "id": "margin_top",
          "label": "Top margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "margin_bottom",
          "label": "Bottom margin",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    },
    {
      "type": "divider",
      "name": "Divider",
      "limit": 5,
      "settings": [
        {
          "type": "select",
          "id": "style",
          "label": "Style",
          "options": [
            { "value": "line", "label": "Line" },
            { "value": "space", "label": "Space" }
          ],
          "default": "line"
        },
        {
          "type": "range",
          "id": "padding_top",
          "label": "Top padding",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 10
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "label": "Bottom padding",
          "min": 0,
          "max": 40,
          "step": 2,
          "unit": "px",
          "default": 10
        },
        {
          "type": "header",
          "content": "Layout"
        },
        {
          "type": "select",
          "id": "position",
          "label": "Position",
          "options": [
            { "value": "body", "label": "Body (above items)" },
            { "value": "footer", "label": "Footer (below items)" }
          ],
          "default": "footer"
        },
        {
          "type": "color",
          "id": "block_bg_color",
          "label": "Background color"
        },
        {
          "type": "color",
          "id": "block_text_color",
          "label": "Text color"
        }
      ]
    }
  ]
}
{% endschema %}
